# Supabase (фронтовая синхронизация прогресса).
# anon-ключ ПУБЛИЧНЫЙ — его можно вшивать в сборку (доступ защищён валидацией
# initData на сервере + RLS). Реальные значения клади в .env (он в .gitignore).
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
